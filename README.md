# Mouse Wheel Mission Control

Персональная утилита для macOS, которая открывает Mission Control при нажатии на колесо мыши.

## Описание

Эта утилита работает в фоновом режиме и автоматически открывает Mission Control при нажатии на среднюю кнопку мыши (колесо). Идеально подходит для быстрого переключения между рабочими пространствами.

## Возможности

- 🖱️ **Перехват нажатий колеса мыши** - отслеживает нажатия средней кнопки мыши глобально
- 🚀 **Автоматический запуск** - запускается при входе в систему
- 🔄 **Автоматический перезапуск** - перезапускается при сбоях
- 🛡️ **Безопасность** - работает только на вашем Mac, не требует регистрации разработчика
- ⚡ **Быстрая работа** - минимальное потребление ресурсов

## Требования

- macOS 10.15+ (Catalina и новее)
- Python 3.8+
- [UV](https://docs.astral.sh/uv/getting-started/installation/) - современный менеджер пакетов Python

## Установка

### 1. Клонирование и настройка

```bash
git clone https://github.com/yourusername/mouse-wheel-control.git
cd mouse-wheel-control
```

### 2. Автоматическая установка

```bash
./install.sh
```

Скрипт установки автоматически:
- Установит зависимости через UV
- Настроит автозапуск через LaunchAgent
- Загрузит службу в систему

### 3. Настройка разрешений

При первом запуске macOS запросит разрешение на "Универсальный доступ":

1. Откройте **Системные настройки** → **Конфиденциальность и безопасность** → **Универсальный доступ**
2. Добавьте **Terminal** (или **Python**) в список разрешенных приложений
3. Перезапустите службу: `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.user.mousewheelcontrol.plist`

## Использование

После установки утилита работает автоматически:

1. **Нажмите на колесо мыши** - откроется Mission Control
2. **Выберите рабочее пространство** - переключитесь между рабочими столами
3. **Нажмите Escape или кликните** - вернитесь к текущему рабочему пространству

## Проверка работы

### Быстрая проверка
```bash
# 1. Проверьте, что служба запущена
launchctl list | grep mousewheelcontrol

# 2. Проверьте, что процесс Python работает
ps aux | grep mouse_wheel_control

# 3. Нажмите на колесо мыши - должен открыться Mission Control
```

### Полная диагностика
```bash
# Проверка синтаксиса конфигурации
plutil -lint ~/Library/LaunchAgents/com.user.mousewheelcontrol.plist

# Проверка путей в конфигурации
ls -la /Users/alxmelekhin/git/mouse_wheel_control/.venv/bin/python
ls -la /Users/alxmelekhin/git/mouse_wheel_control/src/mouse_wheel_control/main.py

# Проверка логов
tail -f /tmp/mousewheelcontrol.log
tail -f /tmp/mousewheelcontrol.error.log
```

### Тестирование в режиме разработки
```bash
# Запуск в терминале для отладки
uv run python src/mouse_wheel_control/main.py
```

## Управление службой

### Проверка статуса
```bash
launchctl list | grep mousewheelcontrol
```

### Остановка службы
```bash
launchctl bootout gui/$(id -u) ~/Library/LaunchAgents/com.user.mousewheelcontrol.plist
```

### Запуск службы
```bash
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.user.mousewheelcontrol.plist
```

### Просмотр логов
```bash
# Основные логи
tail -f /tmp/mousewheelcontrol.log

# Логи ошибок
tail -f /tmp/mousewheelcontrol.error.log
```

### Диагностика проблем
```bash
# Проверка синтаксиса plist файла
plutil -lint ~/Library/LaunchAgents/com.user.mousewheelcontrol.plist

# Проверка запущенных процессов
ps aux | grep mouse_wheel_control

# Проверка системных логов
log show --predicate 'subsystem == "com.apple.launchd"' --last 5m
```

## Удаление

```bash
./uninstall.sh
```

Скрипт удаления:
- Остановит службу
- Удалит конфигурацию LaunchAgent
- Очистит логи

## Разработка

### Структура проекта
```
mouse_wheel_control/
├── src/
│   └── mouse_wheel_control/
│       └── main.py              # Основной скрипт
├── pyproject.toml               # Конфигурация UV
├── uv.lock                      # Lock-файл зависимостей
├── install.sh                   # Скрипт установки
├── uninstall.sh                 # Скрипт удаления
├── com.user.mousewheelcontrol.plist  # LaunchAgent
└── README.md                    # Документация
```

### Запуск в режиме разработки
```bash
# Активация виртуального окружения
uv shell

# Запуск скрипта
python src/mouse_wheel_control/main.py
```

### Установка зависимостей
```bash
uv sync
```

## Технические детали

### Как это работает
1. **Перехват событий** - использует Core Graphics API через PyObjC для глобального перехвата событий мыши
2. **Фильтрация** - отслеживает только нажатия средней кнопки мыши (buttonNumber == 2)
3. **Вызов Mission Control** - использует AppleScript для эмуляции нажатия F3 (key code 160)
4. **Фоновый режим** - работает через CFRunLoop в отдельном процессе

### Безопасность
- Не требует регистрации в Apple Developer Program
- Использует самоподписанный сертификат
- Работает только с разрешениями пользователя
- Не передает данные в интернет

## Устранение неполадок

### Утилита не реагирует на нажатия
1. Проверьте разрешения в **Системные настройки** → **Конфиденциальность и безопасность** → **Универсальный доступ**
2. Перезапустите службу: `launchctl bootout gui/$(id -u) ~/Library/LaunchAgents/com.user.mousewheelcontrol.plist && launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.user.mousewheelcontrol.plist`
3. Проверьте логи: `tail -f /tmp/mousewheelcontrol.log`

### Служба не запускается
1. Проверьте пути в `com.user.mousewheelcontrol.plist`
2. Убедитесь, что виртуальное окружение создано: `ls -la .venv`
3. Проверьте права доступа: `chmod +x install.sh uninstall.sh`

### Mission Control не открывается
1. Проверьте, что Mission Control работает вручную (F3)
2. Проверьте настройки Mission Control в **Системные настройки** → **Mission Control**
3. Убедитесь, что не отключены горячие клавиши

## Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

## Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста, ознакомьтесь с [CONTRIBUTING.md](CONTRIBUTING.md) для получения информации о том, как внести свой вклад.

## Статус проекта

✅ **Проект полностью реализован и протестирован**

- ✅ Создан Python-скрипт с перехватом событий мыши
- ✅ Настроен LaunchAgent для автозапуска
- ✅ Протестирована работа службы (PID активен)
- ✅ Проверена корректность конфигурации plist
- ✅ Утилита успешно запускается при входе в систему

## Поддержка

При возникновении проблем:
1. Проверьте логи: `/tmp/mousewheelcontrol.log`
2. Убедитесь в правильности разрешений
3. Перезапустите службу
4. При необходимости переустановите утилиту

### Готовые команды для диагностики
```bash
# Полная проверка статуса
launchctl list | grep mousewheelcontrol && \
ps aux | grep mouse_wheel_control && \
plutil -lint ~/Library/LaunchAgents/com.user.mousewheelcontrol.plist
```

