# Вклад в проект Mouse Wheel Mission Control

Спасибо за интерес к проекту! Мы приветствуем любой вклад в развитие утилиты.

## Как внести вклад

### 1. Форк и клонирование

1. Сделайте форк репозитория на GitHub
2. Клонируйте ваш форк:
   ```bash
   git clone https://github.com/yourusername/mouse-wheel-control.git
   cd mouse-wheel-control
   ```

### 2. Настройка окружения

```bash
# Установите UV (если еще не установлен)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установите зависимости
uv sync

# Активируйте виртуальное окружение
uv shell
```

### 3. Внесение изменений

1. Создайте новую ветку для ваших изменений:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. Внесите необходимые изменения
3. Протестируйте изменения:
   ```bash
   # Запуск в режиме разработки
   uv run python src/mouse_wheel_control/main.py
   ```

### 4. Тестирование

Перед отправкой pull request убедитесь, что:

- [ ] Код работает на вашей системе
- [ ] Нет синтаксических ошибок
- [ ] Логика перехвата мыши работает корректно
- [ ] Mission Control открывается при нажатии на колесо мыши

### 5. Отправка изменений

1. Зафиксируйте изменения:
   ```bash
   git add .
   git commit -m "Описание ваших изменений"
   ```

2. Отправьте изменения:
   ```bash
   git push origin feature/your-feature-name
   ```

3. Создайте Pull Request на GitHub

## Рекомендации по коду

### Стиль кода
- Используйте Python 3.8+ синтаксис
- Следуйте PEP 8 для форматирования
- Добавляйте docstrings для функций и классов
- Используйте понятные имена переменных

### Структура проекта
```
src/mouse_wheel_control/
├── main.py              # Основной скрипт
├── __init__.py          # Пакет Python
└── utils.py             # Вспомогательные функции (если нужны)
```

### Тестирование
- Тестируйте на разных версиях macOS
- Проверяйте работу с разными типами мышей
- Убедитесь, что утилита не конфликтует с другими приложениями

## Типы вкладов

### 🐛 Исправление ошибок
- Исправление багов в перехвате событий мыши
- Улучшение обработки ошибок
- Исправление проблем с автозапуском

### ✨ Новые функции
- Поддержка дополнительных жестов мыши
- Настройка горячих клавиш
- Улучшение интерфейса

### 📚 Документация
- Улучшение README.md
- Добавление примеров использования
- Перевод на другие языки

### 🔧 Улучшения
- Оптимизация производительности
- Улучшение логирования
- Рефакторинг кода

## Сообщение об ошибках

При сообщении об ошибке укажите:

1. **Версию macOS**: `sw_vers`
2. **Версию Python**: `python --version`
3. **Версию UV**: `uv --version`
4. **Описание проблемы**: что происходит и что ожидается
5. **Шаги для воспроизведения**: как воспроизвести ошибку
6. **Логи**: содержимое `/tmp/mousewheelcontrol.log` и `/tmp/mousewheelcontrol.error.log`

## Вопросы и обсуждения

- Используйте GitHub Issues для вопросов и обсуждений
- Для срочных вопросов можно связаться через email
- Будьте вежливы и конструктивны в общении

## Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш код будет распространяться под лицензией MIT.

## Благодарности

Спасибо всем участникам, которые помогают развивать проект!

---

**Удачного кодирования!** 🚀
